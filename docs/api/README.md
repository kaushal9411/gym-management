# API Documentation

REST endpoint documentation for the Express backend. The OpenAPI spec is exported
here per version (`v1.yaml`, `v2.yaml`) from the Swagger annotations in
`apps/api/src/modules/*/presentation`. Human-written guides (pagination, error
envelope, idempotency, rate limits) also live here.
