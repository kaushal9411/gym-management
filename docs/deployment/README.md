# Deployment Documentation

Environment topology (dev / staging / production), AWS + Cloudflare setup,
runbooks (deploy, rollback, DR drills, region evacuation), and on-call playbooks.
See `infrastructure/` for the executable counterparts.
