# Database Documentation

Schema documentation, ERDs, migration playbooks (expand → migrate → contract),
partitioning strategy, and multi-tenant isolation notes (tenant_id + RLS).
Source of truth for the schema itself is `apps/api/prisma/schema.prisma`.
