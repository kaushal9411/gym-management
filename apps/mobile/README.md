# @gym-saas/mobile (Flutter)

One Flutter app serves every tenant — tenant identity is data (slug/QR onboarding),
not build config. Run `flutter create .` once locally to generate the android/ and
ios/ host projects (kept out of the scaffold intentionally), then `flutter pub get`.

Folder anatomy is documented in the root README (§ Folder Structure).
