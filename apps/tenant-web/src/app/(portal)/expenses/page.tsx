import { TrendingDown } from 'lucide-react';

import { ComingSoon } from '@/components/coming-soon';

export default function ExpensesPage() {
  return <ComingSoon title="Expenses" icon={TrendingDown} />;
}
